<% if(blogs.length){ %> <% blogs.map(blog => {%>
<div class="card">
  <h1><%= blog.title %></h1>
  <p><%= blog.intro %></p>
  <p><%= blog.body %></p>
</div>
<% }) %> <% } else {%>
<h2>No Blogs found</h2>
<% } %>
