<!DOCTYPE html>
<html lang="en">
  <%- include("./components/head.ejs") %>
  <body>
    <%- include("./components/navbar.ejs") %> <% if(blogs.length){ %> <%
    blogs.map(blog => {%>
    <h1><%= blog.title %></h1>
    <p><%= blog.intro %></p>
    <% }) %> <% } else {%>
    <h2>No Blogs found</h2>
    <% } %>
  </body>
</html>
